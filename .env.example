# Local development uses "/" by default, so these values are optional locally.
# Set them when testing a GitHub Pages project-repository path.
SITE_URL=https://YOUR-USERNAME.github.io
BASE_PATH=/YOUR-REPOSITORY-NAME
